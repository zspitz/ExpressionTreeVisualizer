<ag:AutoGrid x:Class="ExpressionTreeVisualizer.VisualizerDataControl" ColumnCount="3" Columns="*,Auto,*" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:my="clr-namespace:ExpressionTreeVisualizer" xmlns:ag="clr-namespace:WpfAutoGrid;assembly=WpfAutoGrid" xmlns:ygoe="clr-namespace:System.Windows.Controls;assembly=MultiSelectTreeView">
    <FrameworkElement.Resources>
        <my:RootConverter x:Key="RootConverter" />
        <my:ConditionalFormatConverter x:Key="ConditionalFormatConverter" />
        <HierarchicalDataTemplate x:Key="ExpressionNodeTemplate" ItemsSource="{Binding Value.Children}">
            <TextBlock>
                <TextBlock.ToolTip>
                    <ag:AutoGrid ColumnCount="2" Columns="65,Auto" RowCount="5">
                        <TextBlock Text="Path" />
                        <TextBlock Text="{Binding Key}" FontWeight="Bold" />
                        <TextBlock Text="Node type" />
                        <TextBlock Text="{Binding Value.NodeType}" FontWeight="Bold" />
                        <TextBlock Text="Type" />
                        <TextBlock Text="{Binding Value.ReflectionTypeName}" FontWeight="Bold" />
                        <TextBlock Text="Name" />
                        <TextBlock Text="{Binding Value.Name}" FontWeight="Bold" />
                        <TextBlock Text="Value" />
                        <TextBlock Text="{Binding Value.StringValue}" FontWeight="Bold" />
                    </ag:AutoGrid>
                </TextBlock.ToolTip>
                <Run Foreground="DarkGray" Text="{Binding Key, Mode=OneTime, Converter={StaticResource ConditionalFormatConverter}, ConverterParameter=\{0\} -}" />
                <Run Text="{Binding Value.NodeType, Mode=OneTime}" FontWeight="Bold" />
                <Run Text="{Binding Value.ReflectionTypeName, Mode=OneTime, Converter={StaticResource ConditionalFormatConverter}, ConverterParameter= ({0})}" />
                <Run Text="{Binding Value.Name, Mode=OneTime, Converter={StaticResource ConditionalFormatConverter}, ConverterParameter= \{0\}}" />
                <Run Text="{Binding Value.StringValue, Mode=OneTime, Converter={StaticResource ConditionalFormatConverter}, ConverterParameter=\= {0}}" />
            </TextBlock>
        </HierarchicalDataTemplate>
    </FrameworkElement.Resources>
    <ygoe:MultiSelectTreeView ItemsSource="{Binding NodeData, Converter={StaticResource RootConverter}}" ItemTemplate="{StaticResource ExpressionNodeTemplate}" Name="tree">
        <ygoe:MultiSelectTreeView.ItemContainerStyle>
            <Style TargetType="ygoe:MultiSelectTreeViewItem">
                <Setter Property="IsExpanded" Value="True" />
                <Setter Property="ItemIndent" Value="18" />
                <Setter Property="IsSelected" Value="{Binding Value.IsSelected, Mode=TwoWay}" />
            </Style>
        </ygoe:MultiSelectTreeView.ItemContainerStyle>
    </ygoe:MultiSelectTreeView>
    <GridSplitter HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="SlateGray" ShowsPreview="True" Width="5" Grid.Column="1" />
    <DockPanel Grid.Column="2">
        <ag:AutoGrid ColumnCount="3" RowCount="2" Orientation="Vertical" Columns="*,*,2*" DockPanel.Dock="Bottom" x:Name="agEndNodes">
            <ag:AutoGrid.Resources>
                <Style TargetType="Label">
                    <Setter Property="Background" Value="Gray" />
                </Style>
                <Style TargetType="DataGrid">
                    <Setter Property="IsReadOnly" Value="True" />
                    <Setter Property="AutoGenerateColumns" Value="False" />
                </Style>
                <Style TargetType="DataGridRow">
                    <Style.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="{x:Static SystemColors.HighlightColor}" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="{x:Static SystemColors.HighlightTextColor}" />
                    </Style.Resources>
                </Style>
            </ag:AutoGrid.Resources>
            <Label Content="Constants" />
            <DataGrid ItemsSource="{Binding Constants}" SelectedValuePath="Key">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Key.Type}" Header="Type" />
                    <DataGridTextColumn Binding="{Binding Key.Value}" Header="Value" />
                </DataGrid.Columns>
            </DataGrid>
            <Label Content="Parameters" />
            <DataGrid ItemsSource="{Binding Parameters}" SelectedValuePath="Key">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Key.Name}" Header="Name" />
                    <DataGridTextColumn Binding="{Binding Key.Type}" Header="Type" />
                </DataGrid.Columns>
            </DataGrid>
            <Label Content="Closed-over variables" />
            <DataGrid ItemsSource="{Binding ClosedVars}" SelectedValuePath="Key">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Key.Closure}" Header="Closure" />
                    <DataGridTextColumn Binding="{Binding Key.Name}" Header="Name" />
                    <DataGridTextColumn Binding="{Binding Key.Type}" Header="Type" />
                    <DataGridTextColumn Binding="{Binding Key.Value}" Header="Value" />
                </DataGrid.Columns>
            </DataGrid>
        </ag:AutoGrid>
        <TextBox Text="{Binding Source}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="14" Name="source" IsInactiveSelectionHighlightEnabled="True" SelectionBrush="Blue" />
    </DockPanel>
</ag:AutoGrid>
