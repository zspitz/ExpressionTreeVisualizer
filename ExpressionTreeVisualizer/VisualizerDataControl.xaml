<Grid x:Class="ExpressionTreeVisualizer.VisualizerDataControl" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:my="clr-namespace:ExpressionTreeVisualizer" xmlns:ygoe="clr-namespace:System.Windows.Controls;assembly=MultiSelectTreeView">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    
    <FrameworkElement.Resources>
        <my:RootConverter x:Key="RootConverter" />
        <my:ConditionalFormatConverter x:Key="ConditionalFormatConverter" />
        <HierarchicalDataTemplate x:Key="ExpressionNodeTemplate" ItemsSource="{Binding Value.Children}">
            <TextBlock>
                <TextBlock.ToolTip>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="65" />
                            <ColumnDefinition Width="Auto" TextBlock.FontWeight="Bold" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        
                        <TextBlock Text="Path" />
                        <TextBlock Text="{Binding Key}" Grid.Column="1" />
                        <TextBlock Text="Node type" Grid.Row="1" />
                        <TextBlock Text="{Binding Value.NodeType}" Grid.Column="1" Grid.Row="1" />
                        <TextBlock Text="Type" Grid.Row="2"  />
                        <TextBlock Text="{Binding Value.ReflectionTypeName}" Grid.Column="1" Grid.Row="2" />
                        <TextBlock Text="Name" Grid.Row="3"  />
                        <TextBlock Text="{Binding Value.Name}" Grid.Column="1" Grid.Row="3" />
                        <TextBlock Text="Value" Grid.Row="4"  />
                        <TextBlock Text="{Binding Value.StringValue}" Grid.Column="1" Grid.Row="4" />
                    </Grid>
                </TextBlock.ToolTip>
                <Run Foreground="DarkGray" Text="{Binding Key, Mode=OneTime, Converter={StaticResource ConditionalFormatConverter}, ConverterParameter=\{0\} -}" />
                <Run Text="{Binding Value.NodeType, Mode=OneTime}" FontWeight="Bold" />
                <Run Text="{Binding Value.ReflectionTypeName, Mode=OneTime, Converter={StaticResource ConditionalFormatConverter}, ConverterParameter= ({0})}" />
                <Run Text="{Binding Value.Name, Mode=OneTime, Converter={StaticResource ConditionalFormatConverter}, ConverterParameter= \{0\}}" />
                <Run Text="{Binding Value.StringValue, Mode=OneTime, Converter={StaticResource ConditionalFormatConverter}, ConverterParameter=\= {0}}" />
            </TextBlock>
        </HierarchicalDataTemplate>
    </FrameworkElement.Resources>
    <ygoe:MultiSelectTreeView ItemsSource="{Binding NodeData, Converter={StaticResource RootConverter}}" ItemTemplate="{StaticResource ExpressionNodeTemplate}" Name="tree" Grid.Column="0">
        <ygoe:MultiSelectTreeView.ItemContainerStyle>
            <Style TargetType="ygoe:MultiSelectTreeViewItem">
                <Setter Property="IsExpanded" Value="True" />
                <Setter Property="ItemIndent" Value="18" />
                <Setter Property="IsSelected" Value="{Binding Value.IsSelected, Mode=TwoWay}" />
            </Style>
        </ygoe:MultiSelectTreeView.ItemContainerStyle>
    </ygoe:MultiSelectTreeView>
    <GridSplitter HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="SlateGray" ShowsPreview="True" Width="5" Grid.Column="1" />
    <DockPanel Grid.Column="2">
        <Grid DockPanel.Dock="Bottom" x:Name="endNodes">
            <Grid.Resources>
                <Style TargetType="Label">
                    <Setter Property="Background" Value="Gray" />
                    <Setter Property="Grid.Row" Value="0" />
                </Style>
                <Style TargetType="DataGrid">
                    <Setter Property="IsReadOnly" Value="True" />
                    <Setter Property="AutoGenerateColumns" Value="False" />
                    <Setter Property="Grid.Row" Value="1" />
                </Style>
                <Style TargetType="DataGridRow">
                    <Style.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="{x:Static SystemColors.HighlightColor}" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="{x:Static SystemColors.HighlightTextColor}" />
                    </Style.Resources>
                </Style>
            </Grid.Resources>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            
            <Label Content="Constants" />
            <DataGrid ItemsSource="{Binding Constants}" SelectedValuePath="Key">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Key.Type}" Header="Type" />
                    <DataGridTextColumn Binding="{Binding Key.Value}" Header="Value" />
                </DataGrid.Columns>
            </DataGrid>
            <Label Content="Parameters" Grid.Column="1" />
            <DataGrid ItemsSource="{Binding Parameters}" SelectedValuePath="Key" Grid.Column="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Key.Name}" Header="Name" />
                    <DataGridTextColumn Binding="{Binding Key.Type}" Header="Type" />
                </DataGrid.Columns>
            </DataGrid>
            <Label Content="Closed-over variables" Grid.Column="2" />
            <DataGrid ItemsSource="{Binding ClosedVars}" SelectedValuePath="Key" Grid.Column="2">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Key.Closure}" Header="Closure" />
                    <DataGridTextColumn Binding="{Binding Key.Name}" Header="Name" />
                    <DataGridTextColumn Binding="{Binding Key.Type}" Header="Type" />
                    <DataGridTextColumn Binding="{Binding Key.Value}" Header="Value" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <ComboBox Name="cmbLanguages" DockPanel.Dock="Top" HorizontalAlignment="Right" SelectedItem="{Binding Language}" />
        <TextBox Text="{Binding Source}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="14" Name="source" IsInactiveSelectionHighlightEnabled="True" SelectionBrush="Blue" />
    </DockPanel>
</Grid>
